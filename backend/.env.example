# Environment variables for Membership System Backend

# Supabase Configuration
# Get these from your Supabase Dashboard > Settings > API
# 
# ⚠️  API Key Format Support:
# - NEW format (recommended): sb_xxxxx... (starts with "sb_")
# - LEGACY format (still supported): eyJxxxxx... (JWT format)
# Both formats are fully compatible with this application
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database Connection (Supabase PostgreSQL)
# Get this from Supabase Dashboard > Settings > Database > Connection String
# 
# 🔧 Connection Modes (see docs/SUPABASE_DATABASE_CONNECTION_MODES.md for details):
# 
# 1️⃣ TRANSACTION POOLER (推荐用于生产环境) - Port 6543
#    - 支持高并发，无防火墙限制
#    - 适合无服务器环境（Vercel, Netlify, AWS Lambda）
#    DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
# 
# 2️⃣ DIRECT CONNECTION (用于开发和迁移) - Port 5432
#    - 最快性能，支持所有 PostgreSQL 特性
#    - 可能有防火墙限制，连接数有限
#    DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"
# 
# 3️⃣ SESSION POOLER (特殊场景) - Port 5432 + pgbouncer=true
#    - 支持预处理语句，但连接数有限
#    DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres?pgbouncer=true"
#
# 📌 使用 Prisma 的推荐配置（同时配置两个）:
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@aws-0-us-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:YOUR_PASSWORD@aws-0-us-west-1.pooler.supabase.com:5432/postgres"

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRATION=7d

# Application
PORT=8000
NODE_ENV=development

# QuickBooks Integration (Optional - configure later)
# Register at https://developer.intuit.com/ to get credentials
# QUICKBOOKS_CLIENT_ID=
# QUICKBOOKS_CLIENT_SECRET=
# QUICKBOOKS_REDIRECT_URI=http://localhost:8000/api/quickbooks/callback
# QUICKBOOKS_ENVIRONMENT=sandbox
# QUICKBOOKS_WEBHOOK_VERIFIER_TOKEN=

# Frontend URL (for OAuth callback redirects)
FRONTEND_URL=http://localhost:3000
